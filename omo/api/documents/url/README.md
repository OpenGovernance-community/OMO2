# External URL documents

External URL documents currently require no dedicated endpoint. Their validation and persistence stay in the shared document lifecycle and the `Document` dbObject.

Future URL-only actions belong in this directory.

window.commonPageScripts = window.commonPageScripts || {};
window.commonPageScripts["/common/assets/admin-edit-map.js"] = function (pageConfig, pageScript) {
(function(){var runMapInit = function(){var mapElement = document.getElementById(("map_" + pageConfig.key + ""));var latInput = document.getElementById(("" + pageConfig.key_ + "lat"));var longInput = document.getElementById(("" + pageConfig.key_ + "long"));if (!mapElement || !latInput || !longInput) { return; }if (mapElement.dataset.leafletReady === '1') { return; }mapElement.dataset.leafletReady = '1';var initialLat = pageConfig.initialLat;var initialLng = pageConfig.initialLng;var defaultCenter = [46.8182, 8.2275];var center = (typeof initialLat === 'number' && typeof initialLng === 'number') ? [initialLat, initialLng] : defaultCenter;var zoom = (typeof initialLat === 'number' && typeof initialLng === 'number') ? 13 : 7;var map = L.map(mapElement).setView(center, zoom);var tileState = { layer: null, theme: null };if (typeof window.commonBindLeafletTheme === 'function') { window.commonBindLeafletTheme(map, tileState); }var marker = null;function updateMarker(lat, lng, shouldCenter){  if (typeof lat !== 'number' || typeof lng !== 'number' || Number.isNaN(lat) || Number.isNaN(lng)) { return; }  if (marker) { marker.setLatLng([lat, lng]); } else { marker = L.circleMarker([lat, lng], {radius: 9, color: '#0f766e', weight: 2, fillColor: '#14b8a6', fillOpacity: 0.85}).addTo(map); }  if (shouldCenter) { map.setView([lat, lng], Math.max(map.getZoom(), 13)); }}function setCoordinates(lat, lng, shouldCenter){  latInput.value = Number(lat).toFixed(6);  longInput.value = Number(lng).toFixed(6);  updateMarker(Number(lat), Number(lng), shouldCenter);}map.on('click', function(event){ setCoordinates(event.latlng.lat, event.latlng.lng, true); });function syncInputs(){  var lat = parseFloat(latInput.value);  var lng = parseFloat(longInput.value);  if (Number.isNaN(lat) || Number.isNaN(lng)) { return; }  updateMarker(lat, lng, true);}latInput.addEventListener('change', syncInputs);longInput.addEventListener('change', syncInputs);if (typeof initialLat === 'number' && typeof initialLng === 'number') { updateMarker(initialLat, initialLng, false); }window.setTimeout(function(){ map.invalidateSize(); }, 0);window.setTimeout(function(){ map.invalidateSize(); }, 250);};if (typeof window.commonWhenLeafletReady === 'function') { window.commonWhenLeafletReady(runMapInit); } else { runMapInit(); }})();
};
